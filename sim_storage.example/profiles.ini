# SSH connection profiles — copy this directory to sim_storage/ and edit.
# One [section] per profile; the section name is shown in the profile picker.

[example]
host=192.168.1.10
port=22
user=me
auth=password
password=changeme

# Key auth: put the unencrypted PEM at keys/<key_id>.pem
#[server]
#host=server.local
#port=22
#user=me
#auth=key
#key_id=id_ed25519
